# PlantTracker

PlantTracker is a full-stack application for tracking plant seed collections.

- **Backend**: FastAPI + SQLite  
- **Frontend**: React (Vite) + Material UI  
- **Infrastructure**: Docker + Docker Compose

---

## Prerequisites

- **Docker Desktop**


Installation:
```bash
cd plantTracker
docker compose down
docker compose build --no-cache
docker compose up