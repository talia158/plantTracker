services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: planttracker-api
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
